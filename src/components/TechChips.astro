---
interface Props {
  tech: string[];
}

const { tech } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {tech.map((t) => (
    <span class="font-mono text-[11px] tracking-wide px-2.5 py-1 bg-bg border border-border text-muted rounded-sm">
      {t}
    </span>
  ))}
</div>
