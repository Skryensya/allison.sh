---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`single-folder-wrap ${className}`} data-single-folder>
  <slot />
</div>

<style>
  .single-folder-wrap {
    position: relative;
  }
</style>
