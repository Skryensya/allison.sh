---
import HalftonePolaroid from './HalftonePolaroid';

interface Props {
  src: string;
  alt: string;
  caption?: string;
  credit?: string;
  color?: string;
}

const { src, alt, caption, credit, color } = Astro.props;
---

<HalftonePolaroid 
  client:load
  src={src} 
  alt={alt} 
  caption={caption}
  showCaption={true}
  color={color}
/>
