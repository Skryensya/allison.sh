services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PUBLIC_SITE_URL: ${PUBLIC_SITE_URL:-https://allison.sh}
    ports:
      - "3000:3000"
    restart: unless-stopped
